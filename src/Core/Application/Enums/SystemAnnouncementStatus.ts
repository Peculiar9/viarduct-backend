export enum SystemAnnouncementStatus {
    DRAFT = 'draft',
    SCHEDULED = 'scheduled',
    PUBLISHING = 'publishing',
    COMPLETED = 'completed',
    FAILED = 'failed',
    CANCELLED = 'cancelled'
}

